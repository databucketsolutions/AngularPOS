import { DialogQuestionBase } from './dialog-question-base';

export class DialogDropdownQuestion extends DialogQuestionBase<string> {
  controlType = 'dropdown';
  options: {key: string, value: string}[] = [];

  constructor(options: {} = {}) {
    super(options);
    this.options = options['options'] || [];
  }
}